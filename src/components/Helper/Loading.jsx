// Importa a biblioteca React.
import React from "react";

// Cria um componente chamado Loading.
const Loading = () => {
  return <div>Carregando</div>;
};

export default Loading; // Exporta o componente Loading.
